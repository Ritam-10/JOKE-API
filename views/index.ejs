<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Laugh Harder</title>
    <link rel="icon" href="/images/favicon.ico" >
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>
    <div class="container">
        <div class="heading">
            <h1>Get Jokes As U Wish</h1>
        </div>
        <div class="category">
            <form method="get">
                <p>Choose Category</p>
                <br>
                <br>
                <div class="buttons">
                    <input type="submit" class="btn" value="Any" id="any" formaction="/any">
                    <input type="submit" class="btn" value="Misc" id="misc" formaction="/misc">
                    <input type="submit" class="btn" value="Programming" id="programming" formaction="/programming">
                    <input type="submit" class="btn" value="Dark" id="dark" formaction="/dark">
                    <input type="submit" class="btn" value="Pun" id="pun" formaction="/pun">
                    <input type="submit" class="btn" value="Spooky" id="spooky" formaction="/spooky">
                    <input type="submit" class="btn" value="Christmas" id="christmas" formaction="/christmas">
                </div>
                
                <br>
                <br>
                <% if (data.type === "twopart") { %> 
                    <div class="firstPart">
                        <h1> <%= data.setup %>  </h1>
                    </div>
                    <div class="secondPart">
                        <h2><%= data.delivery %></h2>
                    </div>
                <% } else if (data.type === "single") { %>
                    <div class="firstPart">
                        <h1> <%= data.joke %></h1>
                    </div>
                <% } else { %>
                    <p></p>
                <% } %>
            </form>
        </div>
    </div>
    <div class="bottom">
        <% if (data.type === "twopart" || data.type === "single") { %>
            <p>Hope, you smiled.</p>
        <% } %>
    </div>
    <div class="post">
        <form method="get">
            <input type="submit" id="submit" value="POST" formaction="/create">
        </form>
    </div>
</body>
</html>
